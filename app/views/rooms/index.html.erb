<p id="notice"><%= notice %></p>

<h1>Rooms</h1>

<ul class="mt4 list-unstyled">
  <%= render @rooms %> <!--шаблон rooms/room сам поймет-->
</ul>

<div class="mt-2">
  <%= render 'rooms/form', room: @room %>
</div>
